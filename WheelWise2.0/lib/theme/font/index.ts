import { Poppins } from 'next/font/google'
import { Inter } from 'next/font/google'

export const FontInter = Inter({ subsets: ['latin'], weight: ['400'] })
export const FontPoppin = Poppins({ subsets: ['latin'], weight: ['700'] })
